<script language = "javascript">
 function Abfrage()
 {
 return confirm('bla bla');
 }
 </script>
<?php
    if ( !empty( $this->topicContent))
    {
        echo '<div id = "content"><center><h1>';
        echo $this->topicName;
        echo '</h1></center>Version: ';
        echo '<form action="showtopics" method="get"><select name="ver">';
    
        foreach( $this->topicVersionArray as $topicVersion)
        {
            if ( $this->selectedTopicVersion == $topicVersion)
            {
                echo '<option value = "' . $topicVersion . '" selected = "selected">' . $topicVersion;
            }
            else
            {
                echo '<option value = "' . $topicVersion . '">' . $topicVersion;
            }
        }        
        echo '</select><input type = "hidden" name = "id" value = "' . $_GET['id'] . '"><input type=submit value="go"></form>';
        
        echo '<form action = "closetopic" method = "post"><input type = "submit" value = "schließen" onsubmit = "return confirm("Text")"></form>';
        
        echo '<form action = "invite" method = "post">';
            echo '<input type = "hidden" name = "topicID" value = "' . $_GET['id'] . '" />';
            echo '<input type = "submit" value = "Einladungen versenden" />';
        echo '</form>';
        
        echo $this->topicContent;
        echo '</div>';
        echo '<div id = "comment">';
        
        if ( !empty( $this->CommentRowSet))
        {
            echo '<table border = "1">';
            foreach( $this->CommentRowSet as $CommentRow)
            {
                echo '<tr><td>' . $CommentRow['commentDate'] . '</td>';
                echo '<td>' . $CommentRow['userName'] . '</td>';
                echo '<td>' . $CommentRow['commentText'] . '</td></tr>';
            }
            echo '</table>';
            
            echo '<form action = "showcomments?id=' . $_GET['id'] . '&ver=' . $this->selectedTopicVersion . '&page=1" method = "post">';
                echo '<input type = "submit" value = "zeige alle" />';
            echo '</form>';
        }
        echo '<p>' . $this->createCommentForm;
        echo '</div>';
    }
?>